# 계획형 자문·관계 기반 지식 클러스터 구현 메모

## 서재함과 논문 읽기

논문은 탐색 결과·직접 업로드 어느 경로에서 오더라도 `paper_shelf`의 하나의 자료자산으로 관리한다. 문헌 탐색 작업은 후보 논문의 초록·본문 적합성 정보를 만들고 **서재함에 추가**할 항목을 고르는 단계다. 이 단계에서는 지식카드를 자동 생성하지 않는다.

서재함은 제목·저자·레이블 검색, 핵심 문헌 필터, 검토됨/승인 지식 연결됨 필터를 제공한다. 각 논문에는 다음 이력이 남는다.

- 서재함 유입 및 중요도·읽기 상태 변경
- M1 분석 요약과 연구자 메모
- 원문 기반 읽기 질문, 연구자 첨삭, 카드 승인 안건 승격
- 온톨로지 후보 생성
- 승인 지식카드 연결

따라서 지식카드의 출발점은 논문 전체에 대한 `질문–근거–첨삭` 검토이며, 탐색 결과의 단편적 주장 자동 추출이 아니다.

## 외부 긴 문맥 모델 선택

`GEMINI_API_KEY` 환경변수를 설정하면 공개 논문 본문 작업에서 Gemini API를 선택할 수 있다. 선택 가능한 작업에는 외부 전송 고지와 체크박스가 표시된다.

- 논문 분석 요약과 읽기 질문: 논문별 체크박스
- arXiv 후보의 본문 비교: 탐색 작업 체크박스
- 체크 해제: 기존 로컬 Ollama 사용
- API 키가 없을 때: 외부 경로를 실행하지 않고 안내한다.

연구 노트는 외부 전송 선택을 기본으로 켜지 않는다. `paper_reading` 프롬프트의 마지막에는 한국어·블록 형식·쪽수 근거를 다시 확인하는 출력 점검을 넣어, 긴 본문 뒤에도 출력 계약이 유지되도록 했다.

첫 Ollama 호출은 `/api/show`에서 모델의 Modelfile 기본 파라미터를 읽어 감사 로그 진단에 남긴다. 이는 Ollama Chat UI의 대화별 설정을 읽는 API는 아니다. 따라서 논문 읽기에는 Chat에서 사용한 값과 같은 `num_ctx=8192`를 명시해, 16GB Mac에서 과도한 KV cache·스왑이 발생하지 않게 한다. 다른 요청은 기본적으로 `options`와 `think`를 보내지 않아 모델 기본 설정이 실제 생성을 지배한다. 운영자가 과거 앱 프로필의 `num_predict`, `think` 등을 강제하려면 `OLLAMA_USE_MODEL_DEFAULTS=0`을 설정한다.

논문 읽기는 Ollama의 스트리밍 응답을 즉시 화면에 보인다. 호출 감사 로그에는 `load_seconds`, `prompt_eval_seconds`, `eval_seconds`, `effective_request_options`도 기록하므로 모델 재적재·입력 처리·생성 중 어느 단계가 병목인지 구분할 수 있다.

이전 버전이 만든 13열 `paper_reading_questions` 테이블에는 `research_relevance`, `suggested_ontology`가 `NOT NULL`인 경우가 있다. 저장 시 실제 테이블 열을 조회해 이 두 레거시 열에는 빈 값을 넣고, 현재 11열 스키마에는 넣지 않으므로 기존 데이터베이스를 삭제하거나 재생성할 필요가 없다.

논문 읽기는 한 번의 충분한 길이의 응답에서 자유 서술형 연구 서머리와, 연구자가 첨삭할 질문·잠정 답변·근거·유보를 함께 만든다. 서머리는 연구 문제·방법·결과·의미·한계를 충분히 설명하되 고정된 필드 형식을 요구하지 않는다. 요약은 서재함의 논문 정보 바로 아래에 먼저 표시되며, 생성된 모델 원문 전체도 접어서 확인할 수 있다. 질문은 별도의 연구자 검토 항목으로 저장된다. `Question:` 형식뿐 아니라 `**질문 1**`처럼 구분선이 빠진 Markdown 제목형 연속 질문도 각 항목으로 분리해 저장한다. 별도의 2차 재독해는 기본 흐름에서 제거했다.

지식카드는 읽기 질문이 아니라 **연구자가 확정한 Claim 자체를 제목으로** 저장한다. 읽기 질문은 그 Claim을 만든 독해 렌즈이고, 원문 근거와 연구자 첨삭은 카드의 보충 설명·출처로 남는다. 레이블은 검색·관리용 분류어, 핵심 개념은 이후 온톨로지에서 관계를 만들 도메인 개념이다. 적용 대상은 Claim이 다루는 객체·상황·과업 유형이고, 적용 조건은 그 Claim이 성립한 전제·관찰 범위다. 근거 수준은 원 논문의 직접 실증, 이론적 논증, 문헌 종합, 연구자 잠정 해석을 구분해 선택한다.

읽기 질문은 지식카드가 아니라 논문 해석을 위한 검토 단위다. 연구자가 원문 근거와 첨삭을 검토한 자리에서 `지식카드 등록` 또는 `무관`을 결정한다. 등록을 선택하면 제목·주장·개념·적용 대상·조건·한계·근거 수준을 그 자리에서 입력하고, **추가 승인 없이 즉시** 승인 지식카드로 저장해 M2에 지식 업데이트를 알린다. 따라서 연구위원 데스크는 지식카드 승인함이 아니다.

온톨로지 후보는 질문을 그대로 저장하지 않고 연구자가 일반화한 개념·관계·조건 표현을 기록한다. M1의 **온톨로지 구성** 탭은 모든 논문의 후보를 한 작업대에 모아 수정·상태 변경하고 관련 카드를 대조하는 화면이다. 이 후보만 연구위원 데스크에서 승인한 뒤 M1의 관계·계보 정리에서 도메인 온톨로지로 편입할지 검토한다. 후보 자체를 곧바로 M2의 추론 규칙이나 동적 명세에 주입하지는 않는다.

온톨로지 후보를 검토할 때는 해당 논문에서 직접 등록한 지식카드를 먼저 보여준다. 이어 승인 관계로 연결된 기존 카드와, 후보 표현 및 논문 카드의 Claim·개념·조건을 질의로 찾아낸 의미·키워드 유사 카드도 함께 제시한다. 이 탐색은 후보를 자동 확정하지 않고, 연구자가 공통 개념·관계·조건 및 충돌·차이를 판단하기 위한 작업 컨텍스트다.

지식카드 등록·무관 판단과 온톨로지 후보 기록은 M1의 자동 처리 로그가 아니라 **연구자 행동 일화**다. 각 행동은 논문, 읽기 질문, 원문 근거, 연구자 결정, 실제 작성 단계, 등록 카드 또는 일반화 표현을 포함한 `paper_card_registration` 또는 `ontology_curation` 에피소드로 저장한다. 다음 유사 지식카드화·온톨로지 작업을 열면 이 선례를 먼저 리콜해 보여주고, 당시 등록·참조 카드 및 미결 사항을 함께 확인하게 한다. 선례는 현재의 원문 근거를 대체하지 않는다.

## 이번 변경의 핵심

질문과 외부 자문은 더 이상 `질문 → 상위 카드 검색 → 답변`으로 처리하지 않는다.

1. M2가 최종 판단과 최대 3개의 하위 질문으로 이루어진 답변 계획을 만든다.
2. 각 하위 질문마다 검색기가 임베딩 유사도로 시드 후보를 찾고, 카드 상태·제외 범위 규칙을 적용한다. 로컬 임베딩을 사용할 수 없으면 lexical 검색으로 자동 전환한다.
3. 승인된 `KnowledgeRelation`을 따라 1-hop, 제한된 2-hop 카드까지 확장한다.
4. 클러스터의 카드·관계 경로를 근거로 하위 판단을 만들고, 이를 최종 답변으로 통합한다.

LLM은 계획, 하위 판단, 최종 서술을 담당한다. 임베딩은 시드 후보 발견에만 사용한다. 후보의 상태·제외 범위 필터와 관계 확장은 `KnowledgeRetriever.cluster()`의 결정론적 코드가 담당한다.

## 호환성

`KnowledgeCard`에는 `concepts`, `applies_to`, `excludes`,
`supports_question_types`, `evidence_level`, `status`가 추가되었다. 모두 기본값이
있으므로 기존 승인 JSONL 카드는 그대로 읽힌다. 신규 카드 지식화 UI에 이 필드를
직접 편집하는 단계는 다음 작업으로 남겨 두었다.

## 화면 사용 흐름

- **M2 · 연구자 질문 대응**: `답변 계획 만들기` → 계획과 하위 질문 확인 →
  `계획에 따라 근거 수집·연구 의견 만들기`
- **외부 자문 대응**: 요청 해석 확인 → `자문 답변 계획 만들기` →
  `계획에 따라 근거 수집·자문 답변 만들기`

두 화면 모두 카드별 0/1/2-hop 경로를 펼쳐 볼 수 있다. 최종 사례 기록에는
`evidence_card_ids`, `evidence_relation_ids`, `subquestion_judgments`가 남는다.

## 다음 구현 권고

1. M1 후보 카드 작성·승인 UI에서 새 검색 필드를 직접 보완할 수 있게 한다.
2. 근거 클러스터가 비었거나 상충 카드만 있을 때 자동으로 M1 탐색 Intent 후보를
   만들고 연구자 승인함으로 보낸다.
3. 사례 타임라인에서 계획, 하위 판단, 지식 클러스터를 한 화면으로 투영한다.
4. 실제 Ollama·Streamlit 의존성이 설치된 Mac 환경에서 UI 통합 테스트를 수행한다.

## 일화적 메모리와 사례 리콜

`cases`, `phenomena`, `decisions`는 사건 원장으로 유지한다. 계획형 연구자 질문 또는 외부 자문이 답변까지 완료되면, 해당 사례의 질문·맥락·판단 질문·계획·답변 요약·근거 ID·미결 사항을 `episode_memories`에 저장한다.

`EpisodicRetriever`는 이 요약의 `retrieval_text`를 임베딩한다. 이 텍스트에서는 **상황과 판단 질문이 중심**이고 답변 요약은 보조 정보다. 새 요청도 같은 형태의 상황 텍스트로 임베딩하여 cosine similarity 상위 3개 사례를 리콜한다.

- 임베딩이 가능하면: `0.9 × situation embedding cosine + 0.1 × lexical overlap`
- 임베딩이 불가능하면: lexical overlap으로만 안전하게 대체
- `superseded` 사례는 리콜에서 제외
- 리콜한 답변은 선례이며 현재의 증거가 아니다. 그 사례가 사용한 카드 중 아직 활성인 카드만 `revalidation_card_ids`로 동적 명세에 전달한다.

과거 사례가 `confirmed`이고 유사도가 0.82 이상이며 활성 근거카드가 남아 있으면 `precedent_adaptation` 전략을 제안한다. 그렇지 않으면 기본 `planned_investigation` 경로로, 새 계획과 지식클러스터를 만든다.
