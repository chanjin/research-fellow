# Ontology Inline Builder v2

## 변경 목표

온톨로지 타입을 여러 카드에 일괄 부여하는 방식에서, 검색 결과 카드마다 대표 타입을 즉시 지정하고 그 결과를 우측 컨텍스트 그래프에서 확인하는 방식으로 변경한다.

## UX

- `Ontology Builder`
  - 키워드 / 임베딩 / 승인 카드 관계로 카드 탐색
  - 검색 결과 카드별 현재 타입 표시
  - 카드별 타입 드롭다운에서 즉시 타입 변경 / 해제
  - 카드 안에서 새 타입 생성 후 즉시 지정
  - `미분류 카드 우선` 정렬
  - 타입이 지정된 카드가 자동으로 그래프 포커스가 됨
  - 오른쪽에 포커스 타입 + 1-Hop 타입 관계 표시
  - 포커스 타입을 기준으로 새 타입 관계를 즉시 추가
- `Ontology Map`
  - 전체 타입 / 타입 관계 그래프
  - 타입별 카드 목록
  - 전체 구조 관점의 관계 추가 / 삭제

## 데이터 모델

DB는 기존 `ontology_card_assignments` N:M 구조를 유지한다. 다만 현재 Builder UI는 카드당 대표 타입 1개만 사용한다.

`Ledger.set_card_ontology_type(card_id, type_id)`가 현재 카드의 기존 타입 할당을 교체한다. `type_id=None`이면 미지정 상태가 된다.

## 그래프

`ontology_context_dot()`은 선택한 타입과 직접 연결된 1-Hop 타입 및 관계만 투영한다. 전체 온톨로지는 `ontology_dot()`으로 유지한다.

## 테스트

- 카드의 대표 타입 변경 시 기존 타입이 교체됨
- 타입 해제 가능
- 컨텍스트 그래프는 포커스 타입과 1-Hop만 포함
- 기존 ontology CRUD / 검색 확장 테스트 유지
