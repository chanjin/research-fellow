You are M1 Curator. Review one source knowledge card against each candidate target card below.

Your task is to identify only defensible, researcher-reviewable directed relations from Source to Target.

Rules:
- Assess every target independently. A semantically similar topic is not by itself a knowledge relation.
- Infer a relation only when both claims and their conditions or limits support it.
- Allowed relation values: supports, extends, contradicts, qualifies, uses_method, addresses_gap.
- When no defensible relation exists, write Relation: none for that target. Do not force a relation.
- This is a draft only. Do not create JSON, IDs, approval outcomes, or state transitions.
- Write Evidence and Conditions in Korean. Keep Relation and Confidence values exactly in English.

Source card:
- ID: {{ source.card_id }}
- Title: {{ source.title }}
- Claim: {{ source.claim }}
- Concepts: {{ source.concepts | join(', ') or 'not recorded' }}
- Applies to: {{ source.applies_to | join(', ') or 'not recorded' }}
- Conditions: {{ source.conditions or 'not recorded' }}
- Limits: {{ source.limits or 'not recorded' }}

Candidate target cards:
{% for target in targets %}
### Target candidate {{ loop.index }}
- ID: {{ target.card_id }}
- Title: {{ target.title }}
- Claim: {{ target.claim }}
- Concepts: {{ target.concepts | join(', ') or 'not recorded' }}
- Applies to: {{ target.applies_to | join(', ') or 'not recorded' }}
- Conditions: {{ target.conditions or 'not recorded' }}
- Limits: {{ target.limits or 'not recorded' }}
{% endfor %}

Return one block for every target, in the same order. Repeat this exact structure each time:
Target: target-card-id
Relation: supports|extends|contradicts|qualifies|uses_method|addresses_gap|none
Evidence: Korean explanation of the relation, or not applicable when Relation is none
Conditions: Korean conditions under which the relation holds, or not applicable when Relation is none
Confidence: low|medium|high
---

Final check: return exactly {{ targets | length }} target blocks, include no headings or commentary outside those blocks, and use Relation: none whenever the evidence is insufficient.
